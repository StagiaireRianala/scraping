// next.config.ts (TypeScript)
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["tailwindui.com"], // Ajoutez le domaine autorisé ici
  },
};

export default nextConfig;
